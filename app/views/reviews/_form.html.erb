<%= simple_form_for [@cocktail, @review] do |f| %>
  <%= f.input :rating, as: :radio_buttons, collection: 1..5 %>
  <%= f.input :content %>
  <%= f.submit "Review", class: "btn btn-outline-dark" %>
<% end %>
